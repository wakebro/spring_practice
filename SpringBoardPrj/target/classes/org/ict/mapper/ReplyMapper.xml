<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.ict.mapper.ReplyMapper">

	<select id="getList" resultType="org.ict.domain.ReplyVO">
		SELECT * FROM reply_tbl WHERE bno=#{bno} ORDER BY replydate DESC
	</select>
	
	<insert id="create">
		INSERT INTO reply_tbl(rno, bno, reply, replyer)
		VALUES(REPLY_NUM.nextval, #{bno}, #{reply}, #{replyer})
	</insert>
	
	<update id="update">
		UPDATE reply_tbl SET reply=#{reply}, updatedate=SYSDATE WHERE rno=#{rno}
	</update>
	
	<delete id="delete">
		DELETE reply_tbl WHERE rno=#{rno}
	</delete>
	
	<select id="getListWithPaging" resultType="org.ict.domain.ReplyVO">
		SELECT rno, bno, reply, replyer, replyDate, updatedate FROM reply_tbl WHERE bno=#{bno} ORDER BY rno asc
	</select>
</mapper>